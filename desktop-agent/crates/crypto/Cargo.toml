[package]
name = "desksync-crypto"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "End-to-end session encryption for DeskSync: X25519 ECDH → HKDF-SHA256 → AES-256-GCM secure channel with per-direction keys and replay protection. Pure Rust; wire-compatible with the Flutter client."

[dependencies]
x25519-dalek = { workspace = true }
aes-gcm = { workspace = true }
hkdf = { workspace = true }
sha2 = { workspace = true }
zeroize = { workspace = true }
hex = { workspace = true }
base64 = { workspace = true }
thiserror = { workspace = true }
